{
  "name": "concurrency-eval-node",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.mjs",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint-check": "eslint **/*.mjs",
    "lint-fix": "eslint **/*.mjs --quiet --fix",
    "prettier-fix": "prettier --write **/*.mjs",
    "prettier-check": "prettier --check **/*.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.31.0",
    "prettier": "^3.0.0"
  }
}
